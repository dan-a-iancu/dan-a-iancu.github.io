---
title: 'OIT 248 — Week Zero Tutorial'
type: landing

# Unlisted on purpose: this page exists so the two tutorial files can be handed out
# in class by URL. `list: never` keeps it out of the sitemap and any page listings,
# so it is reachable by its address but not advertised or indexed.
build:
  list: never

# The two files live in static/oit248/ rather than in a page bundle, because
# config/_default/hugo.yaml sets `ignoreFiles: [\.ipynb$, ...]` -- a notebook placed
# in content/ would be silently skipped and never published. static/ is not subject
# to that filter.

sections:
  - block: markdown
    content:
      title: ""
      text: |-
        ### Week Zero Tutorial — files

        Download both files below, and **save them in the same folder**. The notebook
        loads `Gradebook.csv` from whatever folder it is run in, so it will not find
        the data if the two are kept apart.

        <p>
        <a href="/oit248/Python_Preparation_and_Week_Zero_Tutorial_OIT248.ipynb" download><strong>Python Preparation and Week Zero Tutorial</strong></a>
        &nbsp;&middot;&nbsp; Jupyter notebook (229 KB)
        </p>

        <p>
        <a href="/oit248/Gradebook.csv" download><strong>Gradebook.csv</strong></a>
        &nbsp;&middot;&nbsp; sample data used by the notebook (1 KB)
        </p>

        Open the notebook in JupyterLab and work through it from the top.
    design:
      columns: "1"
---
